{
  "name": "full-stack-types",
  "version": "1.0.0",
  "description": "Full Stack Types",
  "scripts": {
    "build": "tsc -b -v",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "^3.6.3"
  }
}
